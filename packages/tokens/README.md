# Versable tokens

`src/tokens.json` is the canonical, DTCG-compatible source. Do not edit `dist/` outputs. Consume semantic aliases in applications and component code; primitives are reserved for token composition.
